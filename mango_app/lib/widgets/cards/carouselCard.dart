import 'package:flutter/material.dart';

class CarouselCard extends StatelessWidget {
  String photo;
  CarouselCard(String photo) {
    this.photo = photo;
  }

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
